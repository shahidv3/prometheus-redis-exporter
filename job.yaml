prometheus:
  prometheusSpec:
    additionalScrapeConfigs:
      - job_name: 'redis-exporter'
        scrape_interval: 30s
        static_configs:
          - targets: ['redis-exporter.monitoring.svc:9121']
